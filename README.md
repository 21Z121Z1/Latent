# Latent

Latent is an experimental scene-referred computational photography core for Android. The project is being built around semantic imaging types, a backend-independent imaging graph, a deterministic reference implementation, and a Vulkan production path.

The repository is under active construction; APIs are not yet stable.
